/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com"], // Use the correct hostname for Unsplash images
  },
};

export default nextConfig;
